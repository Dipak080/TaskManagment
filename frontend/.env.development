# Used by `npm run dev` (Vite development mode).
#
# Currently pointed at the LIVE server backend so you can test the local UI
# against real server data. Swap the commented line back to use local WAMP.
VITE_API_BASE=http://localhost/taskops/public/api
# VITE_API_BASE=https://codesksoftwares.com/taskdatabase/public/api
