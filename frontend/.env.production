# Used by `npm run build` (Vite production mode) — live server.
# Frontend (this build) is served from /task ; the CodeIgniter backend lives
# at /taskdatabase/public, so its API is at /taskdatabase/public/api.
VITE_API_BASE=https://codesksoftwares.com/taskdatabase/public/api
