# Base env (loaded in every mode). The API URL is set per-mode instead:
#   .env.development -> local WAMP   (npm run dev)
#   .env.production  -> live domain  (npm run build)
# If VITE_API_BASE is unset, src/api/client.js auto-detects by hostname.
